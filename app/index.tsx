import CalendarPrototype from "@/components/CalendarPrototype/CalendarPrototype";
import React from "react";

type Props = {};

const home = (props: Props) => {
  return <CalendarPrototype />;
};

export default home;
